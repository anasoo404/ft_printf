man 3 printf
man 3 write
man stdarg

malloc
free
write
va_start, va_arg, va_copy, va_end

🟦 Bonus

Tu peux gérer :

- (left align)

0 (zero padding)

.precision

width

# (hex prefix)

(space before positive)

+ (force sign)

Entreprise : ce sont les flags classiques.

/*Jour 1 — Comprendre les variadic functions

→ faire un mini test :

void test(const char *fmt, ...) {
    va_list args;
    va_start(args, fmt);
    int i = va_arg(args, int);
    char *s = va_arg(args, char *);
    va_end(args);
}
Jour 2 — Architecture du projet + Makefile
Jour 3 — %c et %s
Jour 4 — %d, %i, %u
Jour 5 — %x, %X (conversion base 16)
Jour 6 — %p
Jour 7 — tests + correction + norme*/
